{
    "train_micro_batch_size_per_gpu": 8,

    "steps_per_print": 25,

    "optimizer": {
        "type": "Adam",
        "params": {
          "lr": 1e-3,
          "betas": [
            0.9,
            0.999
          ],
          "eps": 1e-8,
          "weight_decay": 3e-7
        }
    }
}